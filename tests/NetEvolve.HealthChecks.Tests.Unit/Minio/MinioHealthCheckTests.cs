namespace NetEvolve.HealthChecks.Tests.Unit.Minio;

using NetEvolve.Extensions.TUnit;

[TestGroup(nameof(Minio))]
public class MinioHealthCheckTests
{
    // Additional unit tests can be added here as needed
    // For now, the main functionality is tested via integration tests
}
